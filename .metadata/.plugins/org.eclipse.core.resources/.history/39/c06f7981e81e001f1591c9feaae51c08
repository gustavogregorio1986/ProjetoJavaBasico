package control;

import entity.Estudo;

public class ControlaEstudo {

	public Integer CalcularEstudo(Estudo estudo) {
		Integer calculo = 0;
		for(int i = 0; i <= estudo.getNumero(); i++) {
			calculo = i + estudo.getNumero();
		}
		
		return calculo;
	}
}
